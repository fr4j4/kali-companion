# Place Piper `.onnx` voice models here.
#
# Each voice has two files: `<voice_id>.onnx` (model) and
# `<voice_id>.onnx.json` (model config). The default voice is
# `es_ES-davefx-medium` referenced by `voice_configs/robot-es.json`.
#
# Download with `scripts/download-voices.sh` (added in Phase 1).