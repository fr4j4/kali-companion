import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrQr({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const d=data as any; const url=typeof d==="string"?d:(d?.url||d?.href||ev.content||""); return <Container flexDirection="column" padding={12} gap={8} alignItems="center"><Container width={120} height={120} backgroundColor="white" borderRadius={8} alignItems="center" justifyContent="center"><Text fontSize={28} color="black">◈</Text></Container><Text fontSize={10} color="#38bdf8">{String(url).slice(0,60)}</Text></Container> }