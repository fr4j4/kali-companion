import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrTerminal({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const raw=ev.content??""; const txt=typeof data==="string"?data:raw; return <Container flexDirection="column" padding={8} gap={4} backgroundColor="#020617" borderRadius={6}><Text fontSize={9} color="#94a3b8">terminal</Text>{txt.split("\n").slice(0,20).map((l,i)=><Text key={i} fontSize={9} color="#22c55e">{l.slice(0,80)}</Text>)}</Container> }