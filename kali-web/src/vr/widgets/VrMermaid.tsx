import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrMermaid({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const raw=ev.content??""; const code=typeof data==="string"?data:raw; return <Container flexDirection="column" padding={10} gap={6} backgroundColor="#0f172a" borderRadius={6}><Text fontSize={9} color="#a78bfa">◈ mermaid</Text><Text fontSize={9} color="#cbd5e1">{code.slice(0,600)}</Text><Text fontSize={8} color="#64748b">preview SVG en canvas 2D</Text></Container> }