import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrImage({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const d=data as any; const url=(d?.url||d?.src||d?.image||ev.content||"") as string; const caption=d?.caption?String(d.caption):""; return <Container flexDirection="column" padding={8} gap={6} alignItems="center"><Container width="100%" height={180} backgroundColor="#1e293b" borderRadius={8} alignItems="center" justifyContent="center"><Text fontSize={10} color="#94a3b8">[imagen] {url.slice(0,44)}</Text></Container><Text fontSize={9} color="#94a3b8">{url.slice(0,60)}</Text>{caption&&<Text fontSize={10} color="#e2e8f0">{caption.slice(0,80)}</Text>}</Container> }