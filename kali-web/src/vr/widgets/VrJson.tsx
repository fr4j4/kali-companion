import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrJson({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const raw=ev.content??""; const txt=typeof data==="string"?data:JSON.stringify(data,null,2)||raw; return <Container flexDirection="column" padding={8} gap={4} backgroundColor="#0f172a" borderRadius={6}><Text fontSize={9} color="#fbbf24">JSON</Text><Text fontSize={9} color="#cbd5e1">{txt.slice(0,800)}</Text></Container> }