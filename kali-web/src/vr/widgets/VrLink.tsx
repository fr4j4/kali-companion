import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrLink({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const d=data as any; const url=typeof d==="string"?d:(d?.url||d?.href||ev.content||""); const title=d?.title?String(d.title):""; return <Container flexDirection="column" padding={12} gap={6} backgroundColor="#1e293b" borderRadius={8}><Text fontSize={12} color="#60a5fa">{title||"Link"}</Text><Text fontSize={10} color="#38bdf8">{String(url).slice(0,80)}</Text></Container> }