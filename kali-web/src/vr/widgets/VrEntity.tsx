import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrEntity({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const d=(data as any)||{}; const name=d.name||d.title||ev.title||ev.windowType; const desc=d.description||d.desc||""; return <Container flexDirection="column" padding={10} gap={8} backgroundColor="#1e293b" borderRadius={8}><Text fontSize={12} color="#38bdf8">{String(name).slice(0,40)}</Text><Text fontSize={10} color="#cbd5e1">{String(desc).slice(0,120)}</Text>{Object.entries(d).filter(([k])=>!["name","title","description","desc"].includes(k)).slice(0,5).map(([k,v])=><Container key={k} flexDirection="row" justifyContent="space-between"><Text fontSize={9} color="#94a3b8">{k}</Text><Text fontSize={9} color="#e2e8f0">{String(v).slice(0,30)}</Text></Container>)}</Container> }