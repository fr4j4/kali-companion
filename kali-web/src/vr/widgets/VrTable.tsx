import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrTable({ev}:{ev:ArtifactEvent}){ const {data}=parseContent(ev); const rows=((data as any)?.rows||[]) as Array<Record<string,unknown>>; if(!rows.length) return <Container padding={10}><Text color="#64748b">(tabla vacía)</Text></Container>; const cols=Object.keys(rows[0]); return <Container flexDirection="column" padding={8} gap={6}><Container flexDirection="row" gap={8} padding={6} backgroundColor="#1e293b" borderRadius={4}>{cols.map(c=><Text key={c} fontSize={10} color="#38bdf8" flexGrow={1}>{c}</Text>)}</Container><Container flexDirection="column" gap={2}>{rows.slice(0,8).map((r,i)=><Container key={i} flexDirection="row" gap={8} padding={6} backgroundColor={i%2?"#0f172a":"#162032"} borderRadius={4}>{cols.map(c=><Text key={c} fontSize={10} color="#e2e8f0" flexGrow={1}>{String((r as any)[c]??"").slice(0,18)}</Text>)}</Container>)}</Container></Container> }