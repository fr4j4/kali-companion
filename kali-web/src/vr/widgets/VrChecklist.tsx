import { Container, Text } from "@react-three/uikit"; import type { ArtifactEvent } from "../../lib/protocol"; import { parseContent } from "../../components/widgets/base/DataWidget";
export function VrChecklist({ev,onAction}:{ev:ArtifactEvent;onAction?:any}){ const {data}=parseContent(ev); const items=((data as any)?.items||[]) as Array<{text:string;done?:boolean}>; return <Container flexDirection="column" padding={8} gap={4}>{items.slice(0,10).map((it,i)=><Container key={i} flexDirection="row" gap={8} padding={8} backgroundColor="#1e293b" borderRadius={6} hover={{backgroundColor:"#334155"}} onClick={()=>onAction?.("toggle",i)}><Container width={14} height={14} borderWidth={1} borderColor={it.done?"#22c55e":"#475569"} backgroundColor={it.done?"#22c55e":"transparent"} borderRadius={3} alignItems="center" justifyContent="center"><Text fontSize={9} color="#022c22">{it.done?"✓":""}</Text></Container><Text fontSize={11} color={it.done?"#64748b":"#e2e8f0"} flexGrow={1}>{it.text}</Text></Container>)}</Container> }